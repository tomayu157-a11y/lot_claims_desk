-r requirements.txt

# Tests and linting
pytest>=8.0
pytest-asyncio>=0.24
ruff>=0.6

# Only needed to capture screenshots of the running app
playwright>=1.45

# Builds DOCX fixtures for the reviewer-file tests
python-docx>=1.1
